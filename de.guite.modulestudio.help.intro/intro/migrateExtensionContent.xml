<?xml version="1.0" encoding="UTF-8"?>
<introContent>
    <extensionContent id="migrate-top-left" style="css/style.css" name="Migrate Extension" path="migrate/@">
        <group style-id="content-group" id="deguitemodulestudio-introLink-group">
            <text>To open a model from 0.6.x delete the .mostdiagram file and open your .mostapp file in a text editor.</text>
            <text>Scroll to the bottom, remove the "viewLayer" section as well as all "defaultDataSource" lines.</text>
            <text>Remove all "controllerLayer" and "controllerContainer" elements so that the application contains "controllers" directly.</text>
            <text>After you saved the file use "File -> Initialize diagram file" inside ModuleStudio 0.7.0.</text>
            <text>Choose your .mostapp file in the wizard and click "next", then "finish".</text>
            <text></text>
            <text>To open a model from an older version please remove all account controllers and search controllers first.</text>
            <text>Afterwards the existing model can be converted by using the menu entry "File > Migrate old model from 0.5.x".</text>
            <text></text>
            <text>If you want create a model for an existing legacy module you can follow the following procedure.</text>
            <text>First get the file pntablesToXML.php from https://github.com/zikula/core/tree/master/tools - then use this script to migrate your old pntables.php file to an xml file.</text>
            <text>Inside ModuleStudio use the menu entry "File > Import xml table definition" which will open a file selection dialog. Choose the xml file you just created. As a result you will get a new application model in the MOST_output/yourmod.mostapp file.</text>
            <text>Now use "File > Initialize diagram file" to open this application model and create a new diagram file for it.</text>
            <text>In the data editor remove unrequired elements, like table prefixes and default identifier fields like primary and foreign keys. Follow validation messages to get remaining stuff sorted properly.</text>
        </group>
    </extensionContent>
    <extensionContent id="migrate-top-right" style="css/style.css" name="Migrate Extension" path="migrate/@">
    </extensionContent>
    <extensionContent id="migrate-bottom-left" style="css/style.css" name="Migrate Extension" path="migrate/@">
    </extensionContent>
    <extensionContent id="migrate-bottom-right" style="css/style.css" name="Migrate Extension" path="migrate/@">
    </extensionContent>
</introContent>